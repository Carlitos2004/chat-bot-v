{
  "name": "grupo-11-chatbot-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix backend",
    "dev": "npm run dev --prefix backend",
    "start": "npm run start --prefix backend",
    "smoke": "node scripts/smoke-test.mjs"
  }
}
