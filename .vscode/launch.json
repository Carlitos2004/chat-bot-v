{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run chatbot",
      "program": "${workspaceFolder}/src/server.js",
      "cwd": "${workspaceFolder}"
    }
  ]
}
